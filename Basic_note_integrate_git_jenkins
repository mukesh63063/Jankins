After created AWS account. 
While create instance there configuration add port 8080 (in the security section costum default)
These are some commands
sudo apt update -y
sudo apt upgrade -y
sudo apt install -y openjdk-17-jdk
java -version
curl -fsSL https://pkg.jenkins.io/debian-stable/jenkins.io-2023.key | sudo tee /usr/share/keyrings/jenkins-keyring.asc > /dev/null
echo "deb [signed-by=/usr/share/keyrings/jenkins-keyring.asc] https://pkg.jenkins.io/debian-stable binary/" | sudo tee /etc/apt/sources.list.d/jenkins.list > /dev/null
sudo apt update -y
sudo apt install -y jenkins
sudo systemctl start jenkins
sudo systemctl enable jenkins
sudo systemctl status jenkins
With public ip :8080. And some configuration follow. 
sudo cat /var/lib/jenkins/secrets/initialAdminPassword (you will get password)
Connect to jenkins.
First create echo code (Simple)
Second basic python code from git 
Configuration ( add git link where code is there and check master or main )
In execute shell (python3 file_name.py --> save apply)

PYTHON CODE WITH GIT (there are in a repo there multiple code, we want to run 1 code one by one, follow these steps) 
================================================================================================
1). set git link
2). python3 file_name/python_file.py  (Project/pytest/simple_test/basic_code.py) ----->  (use python3 pytest/simple_test/basic_code.py)



